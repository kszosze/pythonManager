-------------------
Required Libraries
-------------------

The following code relay in the web.py library to manage all the REST call implemented.
That library can be installed with:

#python -m pip install web.py

